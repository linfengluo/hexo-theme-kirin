
<% if(theme.CDN){ %>
    <% theme.CDN_list.forEach(function(url) { %>
        <script src="<%= url %>"></script>
    <% }); %>
    <% theme.other_list.forEach(function(url) { %>
        <script src="<%= url %>?v=<%- theme.version %>"></script>
    <% }); %>
<% }else if (theme.scripts !== undefined && theme.scripts.length > 0) { %>
    <!-- scripts list from theme config.yml -->
    <% theme.scripts.forEach(function(url) { %>
        <script src="<%= url %>?v=<%- theme.version %>"></script>
    <% }); %>
<% } %>

<% if(theme.baidu_push){ %>
    <%- partial('components/baidu/auto-post') %>
<% } %>